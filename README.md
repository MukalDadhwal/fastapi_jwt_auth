Complete auth library written in python fastapi! 


## Commands to start the server

### without poetry

```bash
uvicorn main:app --reload
```

### with poetry
```
poetry run uvicorn main:app --reload
```